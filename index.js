function add(a, b) {
  var addition = a + b;
  return addition;
}

add(1 + 80);

function subtract(a, b) {
  var subtract = a - b;
  return subtract;
}

subtract(60 - 40);

function multiply(a, b) {
  var multiply = a * b;
  return multiply;
}

multiply(2 * 3.4);

function multiply(a, b) {
  var multiply = a * b;
  return multiply;
}

divide(5 / 2.5);
 var divide = 5 / 2.5;
 return divide;
 }
 
 inc(n)

